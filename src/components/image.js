import React from "react"
// import { useStaticQuery, graphql } from "gatsby"
// import Img from "gatsby-image"

export default Image = ({ imagesrc, imagealt }) => {
  return <img src={imagesrc} alt={imagealt} />
}
